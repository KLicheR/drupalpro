#!/bin/bash
set -e

# ################################################################################ Import Variables
# Make sure you have edited this file
source CONFIG
if [[ ${DEBUG} == TRUE ]]; then set -x; fi

#skeleton for future updates.
echo 'No updates to install.'
